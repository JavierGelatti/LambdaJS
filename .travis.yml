language: node_js
node_js:
  - "node"
cache: npm

script:
  - npm test
  - npm run build

before_deploy:
  - ls | grep -Ev "index.html|bundle.js|styles.css" | xargs rm -rf

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
